const PAGE_WIDTH = 1242;
const PAGE_HEIGHT = 2208;
const REACTION_WIDTH = 300;
const REACTION_HEIGHT = 500;
const REACTION_X = (PAGE_WIDTH - REACTION_WIDTH) / 2;
const REACTION_Y = (PAGE_HEIGHT - REACTION_HEIGHT) / 2;

export default {
  PAGE_WIDTH,
  PAGE_HEIGHT,
  REACTION_WIDTH,
  REACTION_HEIGHT,
  REACTION_X,
  REACTION_Y,
};
